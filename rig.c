#include "array.c"

typedef struct {
  char* code;
  int* values;
} gtg;

int gig (char* code, int* values) {
  gtg *gg;
  gg->code;
  gg->values;
   
}

int gate (gtg *t) {
}

int main () {
  char* tar = getenv("TAR");
  array(tar);
}
